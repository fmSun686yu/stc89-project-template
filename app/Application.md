# app 文件夹 ——— Application（应用层（核心业务逻辑））

## 放什么内容？
> app
   └─ main.c          // 主程序（循环、业务逻辑）
   └─ ui.c / ui.h     // 菜单界面、显示控制（可选）
   └─ app_xxx.c       // 应用逻辑模块（如测温、控制系统）

- 所有单片机的主逻辑都在这里。
- 调用 hal 层的函数来读按键、显示、通信。
- 不直接操作寄存器、不写延时、不使用 GPIO。

## 理解
app 是你的程序真正想做的事情（功能层）。